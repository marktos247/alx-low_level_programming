#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase, followed by a new line
 * Return: 0
 */
int main(void)
{
	char c;

	c = 'x';
	while
		(c <= 'y') {
			putchat (c);
			c++;
		}
	putcher{'\n'};
	return (0);
